include ../content/content
include ../size/size
include ../appearance/appearance
include ../slider/slider

section.order
  h2.heading.order__heading(id="order") Индивидуальный заказ
    em.heading__subheading Соберите торт в 3 шага
  form.order__form(action="https://echo.htmlacademy.ru", method="post", oninput="contentOutput.value=productContent.value;sizeOutput.value=productSize.value;appearanceOutput.value=productAppearance.value")
    ol.order__list
      li.order__item
        h3.order__option Выберите начинку
        .slider.content__slider(id="content-slider")
          +slider-controls("content-prev", "content-next")
          ul.order__sublist.slider__inner
            - for(var i = 0; i < order.content.length; i++)
              li.order__subitem.content.slider__item
                +order-content(order.content[i].img, order.content[i].name, order.content[i].descr, order.content[i].price, i + 1, order.content[i].id)
      |
      |
      li.order__item
        h3.order__option Размер
        .slider.size__slider(id="size-slider")
          +slider-controls("size-prev", "size-next")
          ul.order__sublist.slider__inner
            - for(var i = 0; i < order.size.length; i++)
              li.order__subitem.size.slider__item
                +order-size(order.size[i].sizeImg, order.size[i].mass, order.size[i].diameter, order.size[i].description, order.size[i].id)
      |
      |
      li.order__item
        h3.order__option Оформление
        .slider.appearance__slider(id="appearance-slider")
          +slider-controls("appearance-prev", "appearance-next")
          ul.order__sublist.slider__inner
            - for(var i = 0; i < order.appearance.length; i++)
              li.order__subitem.appearance.slider__item
                +order-appearance(order.appearance[i].img, order.appearance[i].name, order.appearance[i].price, i + 1, order.appearance[i].id)
      |
      |
    include ../summary/summary
